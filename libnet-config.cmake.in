@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

find_dependency(Boost REQUIRED COMPONENTS system)
find_dependency(flatbuffers REQUIRED)
find_dependency(spdlog REQUIRED)

include("${CMAKE_CURRENT_LIST_DIR}/libnet-targets.cmake")

if(NOT TARGET skymarlin::libnet)
    add_library(skymarlin::libnet INTERFACE IMPORTED)
    set_target_properties(skymarlin::libnet PROPERTIES
        INTERFACE_LINK_LIBRARIES libnet
    )
endif()